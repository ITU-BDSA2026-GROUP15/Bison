public record Prop(string Author, int ID, string TaxonID);


public class taxons{
public static void start(string file) {
    
    try{
    using(StreamReader sr = new StreamReader(joined.csv))
        {
            string line;
            while((line=sr.ReadLine()!= null)){
                

            }

        }
    }
    catch(Exception e){
          Console.WriteLine(e.Message);  
        }
}
}